config = {
    verbose = false,
    quiet = false,
    script = 'basic.lua',
    use_alsa = true,
    use_jack = false,
    reconnect = true,
    loop_enable = true,
    main_freq = 25,
    loop_freq = 250,
    watch_freq = 2500,
}
