config = {
    script = 'basic.lua',
    loglevel = 2,
    use_alsa = true,
    use_jack = false,
    reconnect = true,
    loop_enabled = true,
    main_freq = 25,
    loop_freq = 250,
    watch_freq = 2500,
}
